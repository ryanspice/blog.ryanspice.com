# Weekly Dev Log Prep - 2026-06-06 to 2026-06-12

Prepared on 2026-06-12 for a possible weekly public dev-log entry on 2026-06-13.

This is a private source-prep/archive note. It does not publish a public dev-log entry and does not change owner-gated brief visibility.

## Source Boundary

Allowed sources used in this pass:

- Blog repo history and current repo files.
- Existing Codex memory and automation memory.
- AI Wiki readable Markdown/JSON summaries and project-source bundles.
- AI Wiki `.claude` command/skill files.
- AI Wiki `.deepseekgui` Markdown/JSON/JSONL file inventory and metadata sampling only.
- User-level Claude, Hermes, and Codex roots, audited as filesystem metadata and non-secret project-memory surfaces only.

Sources intentionally not read:

- Browser profiles, cookies, session stores, credential-backed app databases, app credentials, or private browser storage.
- DeepSeek GUI SQLite, WAL, or SHM database contents.
- Raw assistant message bodies from DeepSeek JSONL thread logs.
- Claude credential files, Hermes `.env` or auth stores, Codex auth files, and large SQLite/log databases.
- Raw Claude/Hermes/Codex session bodies unless they are already summarized as project memory or rollout summaries.
- Any `.env`, token value, private key, account identifier, or secret-bearing config.

## Chat And Agent Surface Audit

| Surface | Current Finding | Use For Weekly Dev Log? | Notes |
| --- | --- | --- | --- |
| AI Wiki `.claude` | Contains Claude Code instructions, a read-only AI Wiki audit command, and small AI Wiki skill files. No conversation archive found there. | Yes, as workflow evidence. | Good source for "read-only audit first" and AI Wiki canonical-root rules. Do not frame it as Claude conversation history. |
| AI Wiki `.deepseekgui` | Contains DeepSeek GUI runtime/config files, thread JSONL, SQLite database files, and a writing workspace note. Recent thread metadata exists around 2026-06-09 and 2026-06-10. | Cautious, metadata only unless explicitly exported. | Treat JSONL thread/message bodies as private conversation material unless a curated export exists. Do not read SQLite/WAL/SHM files. |
| AI Wiki root `.hermes` | No root `.hermes` folder found in this pass. | No. | Hermes appears as an interactive function in the automation shell, not a readable folder source here. |
| ChatGPT export drop | `01_Raw/conversations/ChatGPT-Exports` currently contains only a README describing curated export workflow. | No new signal. | Good policy note: manually export high-signal conversations, then ingest durable facts. |
| GPT bridge outputs | Contains connector context pack, handoff notes, smoke checks, and local connector documentation. | Yes, as historical workflow source. | Most recent relevant pack is older; useful for "connector/search memory path" but not a fresh weekly work signal. |
| PixelBoats ChatGPT project sources | Contains curated project-source bundle, handoff, feature matrix, update notes, and skills for v0.4.28. | Yes, as project context. | Not fresh this week, but it sharpens the PixelBoats watchlist and project-source policy. |
| Claude user root | Contains active settings, sessions/history metadata, skills/plugins, and large PixelBoats project-memory files updated on 2026-06-11 and 2026-06-12. | Yes, metadata/project memory only. | Do not read credentials. Treat raw sessions/history as private unless explicitly summarized/exported. |
| Hermes user root | Contains non-secret config, skills, skins, dashboard themes, and a PixelBoats/PromptOps/DeepSeek skill bundle. | Yes, as workflow/source-map evidence. | Do not read `.env`. Config should be summarized at the source-surface level, not quoted. |
| Hermes local data root | Large data store with profiles, memories, logs, sessions, skills, cron/hooks, `SOUL.md`, `USER.md`, and state database. Recent lead-profile memory/skill/log activity exists on 2026-06-11 and 2026-06-12. | Yes, metadata and memory summaries only. | Do not read state DB, auth stores, raw request dumps, or raw logs for public output. |
| Codex user root | Contains automations, memories, skills, plugins, sessions/rollout summaries, browser data, auth, config, and large SQLite/log databases. | Yes, memory and rollout summaries only. | Do not read auth, browser data, SQLite DBs, or raw session JSONL unless needed for private evidence and safely summarized. |

## Expanded Local Agent Root Audit - 2026-06-12

This audit was metadata-only unless a file was already a small readable instruction, memory, summary, or curated project-source document.

- Claude user root: about 1,655 files and 172 MB. Active areas include sessions/history metadata, plugins, skills, backups, file history, and PixelBoats project memory. The PixelBoats project-memory lane has fresh June 11-12 activity and should be part of tomorrow's weekly source scan.
- Hermes user root: about 45 files and 84 KB. Active safe areas include non-secret config, skills, skins, and dashboard themes. The useful signal is the PixelBoats/PromptOps/DeepSeek profile/skill bundle, not credential material.
- Hermes local data root: about 112,400 files and 3.05 GB. This is too large for routine deep scans. Use targeted metadata plus safe memory/profile summaries; exclude state DB, auth, raw request dumps, raw logs, `.env`, and cache-like content.
- Codex user root: about 11,669 files and 6.97 GB. Use durable memories, rollout summaries, automation memory, skills, and plugins. Exclude auth, browser data, large SQLite/log DBs, cache, and raw session bodies unless a private evidence check requires a small targeted read.
- AI Wiki hidden/runtime sources: `.claude`, `.deepseekgui`, explicit ChatGPT export drop, GPT bridge outputs, and PixelBoats ChatGPT project sources remain valid source lanes with the same curated/exported-material rule.

Important operational correction: the weekday automation should not treat "chat stuff" as one bucket. It should classify sources into:

1. Curated project memory and rollout summaries: safe to summarize.
2. Skills, plugins, profiles, and handoff docs: safe to summarize.
3. Session/history metadata: useful for signal detection, not for quoting.
4. Raw conversations, logs, request dumps, browser data, databases, and credential/config secrets: excluded from routine dev-log and brief generation.

## Recent Public Dev Log Archive

The last few public entries in `src/lib/dev-log.ts` already cover the strongest public-safe signals:

- 2026-06-08: multilingual/gated blog surface, catalog coverage, and owner-gated auth hardening.
- 2026-06-09: prerendered site runtime, reading controls, copy actions, same-origin back links, scroll progress, table-of-contents state, and locale hash-link routing.
- 2026-06-09: DeepSeek GUI recovery guide rewrite/release with backup-first Windows recovery guidance, sanitized paths, stock imagery, and source links.

Low-signal checkpoints after those entries:

- 2026-06-10: no public entry; private and owner-gated low-signal automation brief.
- 2026-06-11: no public entry; private brief plus missing owner-gated lane backfill.
- 2026-06-12: no public entry; private and owner-gated low-signal watchlist brief.

## Coverage Gaps To Mention Tomorrow

- The automation had not been consistently auditing the local AI Wiki chat/export surfaces. Tomorrow's weekly note can safely say the source policy was tightened to include curated AI Wiki conversation/export folders while still excluding app databases and browser/session storage.
- The user-level Claude/Hermes/Codex roots also need to be included in the scan policy. Tomorrow's brief can say they were added as local agent memory surfaces, with a default to metadata, skills, project memory, rollout summaries, and explicit exports.
- `.claude` should be described as local AI Wiki audit and skill instructions, not conversation history.
- `.deepseekgui` should be described as a local DeepSeek GUI workspace/runtime surface. Use metadata and curated exports only; do not quote raw thread messages or read SQLite files for public summaries.
- ChatGPT memory is not directly available through this repo. Only explicit local exports, connector packs, Codex memory, and curated project-source bundles should count.
- PixelBoats project-source bundles are older, but Claude project memory and Hermes lead-profile metadata show fresh PixelBoats-adjacent activity. Tomorrow's weekly entry should still avoid claiming implementation detail unless a source summary or repo diff confirms it.

## Weekly Public Dev Log Candidate

Use this only if tomorrow's run confirms no new privacy issue and the public surface should receive a weekly process entry.

```ts
{
	date: '2026-06-13',
	title: 'Tighten the weekly source and release notes lane',
	summary:
		'Consolidated the week around the blog runtime, owner-gated briefs, DeepSeek recovery publishing, and a clearer privacy boundary for Claude, Hermes, Codex, DeepSeek, GPT, AI Wiki, and PixelBoats project-memory sources.',
	source: 'Weekly blog process and local AI memory audit',
	accent: '#8cc8ff',
	tags: ['blog', 'automation', 'privacy', 'ai-wiki', 'deepseek', 'developer workflow', 'research'],
	relatedArticleSlugs: [
		'recover-deepseek-gui-conversations-after-update',
		'hermes-deepseek-setup',
		'how-chatgpt-performs-deep-research',
		'openjarvis-local-ai-personal-ai-on-your-pc',
		'pixelboats-water-pipeline-pixi-webgl'
	],
	relatedArticleTags: ['DeepSeek', 'AI Wiki', 'AI agents', 'developer workflow', 'PixelBoats']
}
```

## Tomorrow Checklist

1. Re-read `docs/dev-log-automation.md`.
2. Check whether any commits or meaningful source signals landed after 2026-06-12.
3. Re-scan the safe source surfaces only:
   - Claude local memory: project memory, skills/plugins, and metadata only.
   - Hermes local memory: profiles, skills, memories, non-secret config summaries, and metadata only.
   - Codex memory: automation memory, durable memories, rollout summaries, skills, plugins, and metadata only.
   - AI Wiki `.claude` Markdown/JSON instruction files.
   - AI Wiki `.deepseekgui` file inventory and metadata only, unless a curated export is present.
   - ChatGPT export drop README and any newly added curated exports.
   - GPT bridge outputs and PixelBoats project-source bundles when project context is needed.
4. Do not read SQLite/WAL/SHM files, raw thread message bodies, raw request dumps, browser profiles, cookies, session stores, private app DBs, credentials, auth files, `.env`, or large logs.
5. If publishing the weekly dev-log entry, verify all related article slugs still exist.
6. Run `pnpm check` after editing `src/lib/dev-log.ts`.
7. Run `pnpm run build:blog` if route/content rendering output changes; use the documented `Build-BlogStatic.ps1 -SkipAdapterSync` fallback if adapter sync hangs.

## Suggested Owner Brief Framing

The useful weekly story is not "more automation noise." It is:

- The blog has a more coherent static/runtime and owner-gated brief lane.
- DeepSeek recovery content is public-safe and practical.
- Local AI memory sources are now separated into safe curated inputs, useful metadata, and excluded raw app/runtime storage.
- Tomorrow's weekly entry should be a compact process note, not a dump of chat histories.
