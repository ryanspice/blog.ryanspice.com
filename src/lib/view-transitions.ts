export function articleTitleTransitionName(slug: string): string {
	return `article-title-${slug}`;
}

export function articlePreviewTransitionName(slug: string): string {
	return `article-preview-${slug}`;
}
